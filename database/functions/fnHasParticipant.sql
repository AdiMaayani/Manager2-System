SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE OR ALTER FUNCTION [dbo].[fnHasParticipant](@p NVARCHAR(10))
RETURNS INT
AS
BEGIN
	DECLARE @value INT
	IF ISNULL(@p,'6') = '6' OR len(@p) < 1
	BEGIN
		SET @value = 0
	END
	ELSE
	BEGIN
		SET @value = 1
	END
	RETURN @value
END
GO
