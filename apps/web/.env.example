# Leave empty to use the Vite dev proxy default (/api -> localhost:5162).
# VITE_API_BASE_URL=/api

# Data mode: local = real API, mock = in-memory mock data (no API required)
VITE_APP_DATA_MODE=local
